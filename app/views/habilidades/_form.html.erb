<%= form_for(@habilidade) do |f| %>
  <% if @habilidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@habilidade.errors.count, "error") %> prohibited this habilidade from being saved:</h2>

      <ul>
      <% @habilidade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pessoa_id %><br>
  </div>
  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :nivel %><br>
    <%= f.number_field :nivel %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
